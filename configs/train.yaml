seed: 20260815
device: auto
epochs: 200
learning_rate: 0.001
weight_decay: 0.00001
early_stopping_patience: 25
mixed_precision: false
cuda_memory_fraction: 0.65
cooldown_seconds: 20
thermal_cooldown_every_epochs: 5
thermal_cooldown_seconds: 15
ensemble_size: 5
max_distribution_samples: 512
max_ode_steps_per_transition: 16
batching:
  unit: section
  max_nodes: 4000
  patches_per_transition_per_epoch: 2
loss:
  lambda_distribution: 1.0
  lambda_spatial: 0.001
  lambda_biology: 0.01
  lambda_residual: 0.001
